"""keepalive-api: a lightweight OpenAI-compatible LLM gateway with per-provider
outgoing rate shaping. Over-limit requests are buffered and sent at the
configured cadence rather than rejected."""

__version__ = "0.1.0"
